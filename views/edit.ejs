<!DOCTYPE html>
<html>
  <head>
      <base href="/">
      <link rel="stylesheet" href="vendors/css/bootstrap.min.css" type="text/css" />
      <link rel="stylesheet" href="vendors/css/ionicons.css" />
      <link rel="stylesheet" href="resources/css/index.css" type="text/css" />

    <title>Edit Article</title>
  </head>

  <body>
    <div class="container">
      <h1 class="text-center">Edit "<%= article.title %>"</h1>
      <div class="col-sm-12">
        <form id="edit" action="/<%= currentUser.username %>/<%= article._id %>?_method=PUT" method="POST">
          <div class="form-group">
            <input type="text" placeholder="Article Title" name="article[title]" value="<%= article.title %>" class="form-control">
          </div>
          <div class="form-group">
            <input type ="text" placeholder="Image Url" name="article[image]" value="<%= article.image %>" class="form-control">
          </div>
          <div class="form-group">
              <textarea placeholder="Description" name="article[body]" class="form-control" rows="10"><%= article.body %></textarea>
          </div>
          <div class="form-group">
            <input type="text" placeholder="City" name="article[city]" value="<%= currentUser.username %>" class="form-control" style="display: none;">
          </div>
          <div class="form-group">
            <button class="btn btn-large btn-primary btn-block" type="submit">Submit</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>